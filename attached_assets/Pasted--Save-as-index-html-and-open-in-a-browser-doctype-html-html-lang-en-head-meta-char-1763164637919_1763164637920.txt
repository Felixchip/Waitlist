<!-- Save as index.html and open in a browser -->
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Asymmetric Feature Card Grid</title>
<style>
  /* ========== Theme variables ========== */
  :root{
    --bg: #0f1220;
    --card: #11121a;
    --muted: #9aa0b2;
    --accent: linear-gradient(135deg,#6b5bff 0%, #7ad7ff 100%);
    --radius: 14px;
    --glass: rgba(255,255,255,0.02);
    --card-elev: 0 6px 18px rgba(2,6,23,0.6);
    --gap: 20px;
    --max-width: 1200px;
  }

  /* ========== Page reset ========== */
  * { box-sizing: border-box; }
  html,body { height:100%; }
  body{
    margin:0;
    font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    background: radial-gradient(1200px 700px at 10% 10%, rgba(80,60,140,0.08), transparent),
                linear-gradient(180deg, #0b0d14 0%, var(--bg) 100%);
    color: #e9eef8;
    -webkit-font-smoothing:antialiased;
    -moz-osx-font-smoothing:grayscale;
    padding: 48px 24px;
    display:flex;
    justify-content:center;
  }

  .wrap {
    width: 100%;
    max-width: var(--max-width);
  }

  h1{ margin:0 0 8px 0; font-size:20px; letter-spacing:-0.02em; }
  p{ margin:0; color:var(--muted); font-size:14px; line-height:1.5; }

  /* ========== Grid container ========== */
  .feature-grid {
    display: grid;
    gap: var(--gap);
    align-items: start;
    /* 12-column underlying grid for fine-grained control */
    grid-template-columns: repeat(12, 1fr);
    grid-auto-rows: minmax(120px, auto);
  }

  /* Desktop layout (explicit placement) */
  /* Card positions chosen to match the image: large left, stacked right, bottom-left, bottom-right large */
  .card-1 { grid-column: 1 / span 7; grid-row: 1 / span 2; }   /* big left */
  .card-2 { grid-column: 8 / span 5; grid-row: 1; }             /* top-right */
  .card-3 { grid-column: 8 / span 3; grid-row: 2; }             /* middle-right */
  .card-4 { grid-column: 1 / span 5; grid-row: 3; }             /* bottom-left */
  .card-5 { grid-column: 6 / span 7; grid-row: 3 / span 1; }    /* big bottom-right */
  .card-6 { grid-column: 3 / span 4; grid-row: 2 / span 2; }    /* overlapping small card example */

  /* ========== Card visual style ========== */
  .card {
    background: linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.012));
    border-radius: var(--radius);
    padding: 22px;
    box-shadow: var(--card-elev);
    border: 1px solid rgba(255,255,255,0.03);
    position:relative;
    overflow:hidden;
  }

  /* subtle glass line separators for card content */
  .card .meta { display:flex; gap:12px; align-items:center; margin-bottom:14px; }
  .thumb {
    width:44px; height:44px; border-radius:10px; background:var(--glass);
    display:inline-flex; align-items:center; justify-content:center; font-weight:600;
    font-size:14px; color:var(--muted);
  }

  .title { font-weight:600; font-size:16px; margin-bottom:6px; }
  .desc { color:var(--muted); font-size:13px; line-height:1.5; }

  /* small badge / pin (used for floating messages like in screenshot) */
  .badge {
    position:absolute;
    right:18px;
    top:18px;
    background:var(--accent);
    color:#051326;
    padding:6px 10px;
    border-radius:999px;
    font-weight:700;
    font-size:12px;
    box-shadow:0 6px 20px rgba(60,40,120,0.35);
  }

  /* Example of an overlapping card style (floating above others) */
  .overlap {
    transform: translateY(-18px);
    z-index: 5;
    pointer-events:auto;
  }

  /* Make cards look like 'modules' with inner panels */
  .panel {
    background: linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.015));
    padding:14px;
    border-radius:10px;
    border:1px solid rgba(255,255,255,0.02);
  }

  /* ========== Responsive rules ========== */
  @media (max-width: 980px){
    /* collapse to 8 columns for tablets - simpler reflow */
    .feature-grid { grid-template-columns: repeat(8, 1fr); grid-auto-rows: minmax(100px, auto); }
    .card-1 { grid-column: 1 / span 8; grid-row: 1; }
    .card-2 { grid-column: 1 / span 8; grid-row: 2; }
    .card-3 { grid-column: 1 / span 8; grid-row: 3; }
    .card-4 { grid-column: 1 / span 4; grid-row: 4; }
    .card-5 { grid-column: 5 / span 4; grid-row: 4; }
    .card-6 { grid-column: 1 / span 8; grid-row: 5; transform:none; }
  }

  @media (max-width: 560px){
    /* single column mobile stack */
    .feature-grid { grid-template-columns: 1fr; }
    .card-1, .card-2, .card-3, .card-4, .card-5, .card-6 { grid-column: 1; }
    .card-6 { transform:none; margin-top:-6px; } /* remove floating on mobile */
    body { padding:24px 16px; }
  }

  /* Utility small styles */
  .muted { color: var(--muted); font-size:13px; }
  .kpi { font-weight:700; font-size:20px; }
</style>
</head>
<body>
  <div class="wrap">
    <div class="feature-grid" role="list" aria-label="Feature cards">
      <!-- Big left card -->
      <article class="card card-1" role="listitem" aria-labelledby="t1">
        <div class="meta">
          <div class="thumb">AI</div>
          <div>
            <div id="t1" class="title">AI research assistant</div>
            <div class="muted">Automate data gathering, summarization, and insight generation across sources.</div>
          </div>
        </div>
        <div class="panel" style="margin-top:12px;">
          <p class="muted">100 AI Agents running</p>
          <div style="height:8px; background:rgba(255,255,255,0.03); border-radius:8px; margin-top:10px; overflow:hidden;">
            <div style="width:72%; height:8px; background:linear-gradient(90deg,#6b5bff,#7ad7ff);"></div>
          </div>
        </div>
      </article>

      <!-- Top-right medium -->
      <article class="card card-2" role="listitem" aria-labelledby="t2">
        <div class="badge">Save</div>
        <div class="meta">
          <div class="thumb">🔒</div>
          <div>
            <div id="t2" class="title">Real-time ops monitoring</div>
            <div class="muted">Leverage AI to observe, flag, and optimize workflows as they happen.</div>
          </div>
        </div>
      </article>

      <!-- Middle-right small -->
      <article class="card card-3" role="listitem" aria-labelledby="t3">
        <div class="meta">
          <div class="thumb">☁️</div>
          <div>
            <div id="t3" class="title">Setting up real monitoring</div>
            <div class="muted">5 mins left</div>
          </div>
        </div>
      </article>

      <!-- Bottom-left card -->
      <article class="card card-4" role="listitem" aria-labelledby="t4">
        <div class="meta">
          <div class="thumb">⚙️</div>
          <div>
            <div id="t4" class="title">Custom intelligence modules</div>
            <div class="muted">Tailor AI agents to your team's needs — competitive analysis or internal reporting.</div>
          </div>
        </div>
      </article>

      <!-- Big bottom-right -->
      <article class="card card-5" role="listitem" aria-labelledby="t5">
        <div class="meta">
          <div class="thumb">👤</div>
          <div>
            <div id="t5" class="title">Analysis completed</div>
            <div class="muted">Preparing full report</div>
          </div>
        </div>
      </article>

      <!-- Overlapping floating small card (use .overlap to lift it visually) -->
      <article class="card card-6 overlap" role="listitem" aria-labelledby="t6" style="width:100%;">
        <div class="meta">
          <div class="thumb">💬</div>
          <div>
            <div id="t6" class="title">Live notes & messages</div>
            <div class="muted">Real-time transcription active — collaborators updated.</div>
          </div>
        </div>
      </article>
    </div>
  </div>
</body>
</html>
