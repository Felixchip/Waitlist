<!-- Save as foggy-text.html and open in a browser -->
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Foggy Blurred Bottom Fade</title>
<style>
  /* Basic reset */
  *{box-sizing:border-box;margin:0;padding:0}
  html,body{height:100%}
  body{
    display:flex;
    align-items:center;
    justify-content:center;
    min-height:100vh;
    background: linear-gradient(180deg,#120b2b 0%, #21307a 40%, #7fb2ff 100%);
    font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  }

  /* Container to allow overlapping text layers */
  .hero {
    position: relative;
    line-height: 0.88; /* tighten lines for large text */
    -webkit-font-smoothing:antialiased;
    text-rendering:optimizeLegibility;
  }

  /* Sharp top text */
  .hero .sharp {
    font-weight: 800;
    font-size: clamp(48px, 14vw, 200px); /* responsive size */
    color: #fff;
    letter-spacing: -0.02em;
    position: relative;   /* keep flow */
    display: inline-block;
    z-index: 3;
  }

  /* Blurred duplicate (the 'fog') */
  .hero .fog {
    position: absolute;
    left: 0;
    top: 0;
    font-weight: 800;
    font-size: clamp(48px, 14vw, 200px);
    color: #fff;
    letter-spacing: -0.02em;
    z-index: 2;

    /* heavy blur to create the glow/fog */
    filter: blur(36px);
    transform: translateY(6px); /* tiny nudge to separate slightly — tweak if needed */

    /* fade the bottom of the blurred layer using a mask gradient */
    -webkit-mask-image: linear-gradient(to bottom, rgba(0,0,0,1) 0%, rgba(0,0,0,0.25) 55%, rgba(0,0,0,0) 100%);
    mask-image: linear-gradient(to bottom, rgba(0,0,0,1) 0%, rgba(0,0,0,0.25) 55%, rgba(0,0,0,0) 100%);

    /* reduce opacity so it doesn't overpower the sharp text */
    opacity: 0.95;
    pointer-events: none;
  }

  /* Optional: add a stronger bottom fog (white) that bleeds into background */
  .hero .fog::after{
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: -6%;
    height: 40%;
    pointer-events: none;
    background: linear-gradient(180deg, rgba(255,255,255,0) 0%, rgba(255,255,255,0.12) 40%, rgba(255,255,255,0.55) 100%);
    filter: blur(28px);
    transform: translateY(6px);
    z-index: 1;
    -webkit-mask-image: linear-gradient(to bottom, rgba(0,0,0,0.6) 0%, rgba(0,0,0,0) 100%);
    mask-image: linear-gradient(to bottom, rgba(0,0,0,0.6) 0%, rgba(0,0,0,0) 100%);
  }

  /* Small responsive tweak for very small screens */
  @media (max-width:420px){
    .hero .fog { filter: blur(22px); transform: translateY(4px); }
    .hero .fog::after { filter: blur(18px); }
  }
</style>
</head>
<body>
  <h1 class="hero" aria-hidden="false">
    <span class="sharp">GentheriAI</span>
    <span class="fog">GentheriAI</span>
  </h1>
</body>
</html>
